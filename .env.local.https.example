# Local HTTPS Production Testing Configuration
# This configuration enables testing the app in production mode (DEBUG=False)
# with HTTPS via Caddy reverse proxy.
#
# Setup instructions:
# 1. Copy this file: cp .env.local.https.example .env.local.https
# 2. Update LOCAL_IP to your Mac's local network IP (find it: ipconfig getifaddr en0)
# 3. Add to /etc/hosts: sudo sh -c 'echo "127.0.0.1 dev.wms.local" >> /etc/hosts'
# 4. Run: make local-https
# 5. Access: https://dev.wms.local:8443
# 6. For mobile access: make caddy-export-ca and scan QR code
#
# See docs/LOCAL_HTTPS_SETUP.md for complete instructions.

# =============================================================================
# Local HTTPS Configuration
# =============================================================================
DOMAIN=dev.wms.local
LOCAL_IP=192.168.x.x

# =============================================================================
# Network Access (HTTPS ports)
# NOTE: Update LOCAL_IP in all three settings below when switching networks
# =============================================================================
ALLOWED_HOSTS=dev.wms.local,localhost,192.168.x.x
CSRF_TRUSTED_ORIGINS=https://dev.wms.local:8443,https://localhost:8443,https://192.168.x.x:8443

# =============================================================================
# Django Settings (Production Mode)
# =============================================================================
PORT=8000
DEBUG=False
LOG_LEVEL=INFO
DJANGO_SETTINGS_MODULE=wms.settings
SECRET_KEY=dev-insecure-change-me

# =============================================================================
# Database (copy from deploy/containers.json or use local DB)
# =============================================================================
DB_NAME=
DB_USER=
DB_PASSWORD=
DB_HOST=
DB_PORT=5432
PGSSLMODE=require

# =============================================================================
# AWS S3 (for media storage; set USE_S3=False for local file storage)
# =============================================================================
USE_S3=True
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
